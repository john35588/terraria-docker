name: (Terraria) Build and push Docker image

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Terraria version'
        required: true
        type: string
      version_trim:
        description: 'Terraria version trimmed'
        required: true
        type: string
    
  workflow_call:
    inputs:
      version:
        description: 'Terraria version'
        required: true
        type: string
      version_trim:
        description: 'Terraria version trimmed'
        required: true
        type: string

jobs:
  setup-build-push:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR 
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./terraria
          push: true
          platforms: linux/amd64
          build-args: |
            VERSION=${{ inputs.version }}
            VERSIONTRIM=${{ inputs.version_trim }}
          tags: |
            ghcr.io/john35588/terraria-docker:latest
            ghcr.io/john35588/terraria-docker:terraria-latest
            ghcr.io/john35588/terraria-docker:terraria-${{ inputs.version }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

